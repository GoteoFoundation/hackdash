<div class="form-group">
    <div class="checkbox">
      <label>
        <input type="checkbox" name="{{name}}" id="q-{{_id}}" value="1"{{#if value}} checked="true"{{/if}}> {{title}}
      </label>
    </div>
</div>
